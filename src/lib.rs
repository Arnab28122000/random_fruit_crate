//! # My Crate
//! 
//! `mycrate` is a collection of utilities to make performing certion
//! calculations more convinient.

///Adds one to the number given
/// 
/// # Examples
/// 
/// ```
/// let arg = 5;
/// let answer = arnab_first_crate::add_one(arg);
/// 
/// assert_eq!(6, answer);
/// ```
pub fn add_one(x: i32) -> i32{
    x+1
}